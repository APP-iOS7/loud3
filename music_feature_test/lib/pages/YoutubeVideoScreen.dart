import 'package:flutter/material.dart';

class YoutubeViewScreen extends StatelessWidget {
  const YoutubeViewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("YoutubeVideoTest Page")));
  }
}
